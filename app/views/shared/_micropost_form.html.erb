<% form_for(@micropost, :html => { :class => "micropost_form"} ) do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
    <%= f.text_area :content %> 
    <%= f.submit "Submit" %>
<% end %>
