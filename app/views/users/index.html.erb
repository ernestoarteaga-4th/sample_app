<div id="list">
  <div id="left">  
    <% @users.each do |user| %>
      <%= link_to user.id, :controller => "users", :action => "show", :id => user.id %>
      <br/>
    <% end %>
  </div>
  <div id="right">
    <% @users.each do |user| %>
      <%=user.name%><br/>
    <% end %>
  </div>
</div>

